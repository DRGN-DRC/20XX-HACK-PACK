.ifndef melee.library.included; .include "melee"; .endif
melee.module ppc

.if module.included == 0


  punkpc ppc
  # load punkpc powerpc symbols and macros
  # - this includes several prereq punkpc modules, including enum, strings, and stack objects


.endif
